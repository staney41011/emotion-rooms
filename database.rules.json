{
  "rules": {
    "emotionRooms": {
      "v1": {
        "locked": {
          ".read": true,
          ".write": false,
          ".validate": "newData.isBoolean()"
        },
        "publicWords": {
          ".read": true,
          "$roomId": {
            "$submissionId": {
              ".write": "!data.exists() && newData.exists() && root.child('emotionRooms/v1/locked').val() !== true",
              ".validate": "newData.hasChildren(['roomId','words','createdAt']) && newData.child('roomId').val() === $roomId && newData.child('words').hasChildren(['0']) && newData.child('createdAt').isNumber()",
              "roomId": {
                ".validate": "newData.isString() && newData.val().length <= 40"
              },
              "words": {
                ".validate": "newData.numChildren() >= 1 && newData.numChildren() <= 3",
                "$index": {
                  ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 24"
                }
              },
              "createdAt": {
                ".validate": "newData.isNumber()"
              },
              "$other": {
                ".validate": false
              }
            }
          }
        },
        "privateComments": {
          ".read": false,
          "$roomId": {
            "$submissionId": {
              ".write": "!data.exists() && newData.exists() && root.child('emotionRooms/v1/locked').val() !== true",
              ".validate": "newData.hasChildren(['roomId','comment','createdAt']) && newData.child('roomId').val() === $roomId && newData.child('comment').isString() && newData.child('createdAt').isNumber()",
              "roomId": {
                ".validate": "newData.isString() && newData.val().length <= 40"
              },
              "comment": {
                ".validate": "newData.isString() && newData.val().length <= 180"
              },
              "createdAt": {
                ".validate": "newData.isNumber()"
              },
              "$other": {
                ".validate": false
              }
            }
          }
        },
        "$other": {
          ".read": false,
          ".write": false
        }
      }
    }
  }
}
